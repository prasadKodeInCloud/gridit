function RRectangle(options){	var defaults = {		styles: {			x: 100,			y: 40,			width: 50,			height: 40,			fill: '#8CBAFF',			stroke: '#73ABFF',			strokeWidth: 1,			isDraggable:true,			isLocked:false		},		ponterSides:{			top:true,			right:true,			bottom:true,			left:true,			scale:true		},		pWidth:8	}		var settings = $.extend( true, {}, defaults, options );    var box = new Kinetic.Rect(settings.styles);	var rrectangle = {};	rrectangle.__proto__ = RBox(settings, box, settings.ponterSides);		rrectangle.setSiblings = function(siblings){		box.siblings = siblings;	}		rrectangle.addSibling = function(sibling){		if(!box.siblings){			box.siblings = [];		}		box.siblings.push(sibling);	}		rrectangle.setBottoms = function(bottoms){		box.bottoms = bottoms;	}		rrectangle.addBottom = function(bottom){		if(!box.bottoms){			box.bottoms = [];		}		box.bottoms.push(bottom);	}		rrectangle.setTops = function(tops){		box.tops = tops;	}		rrectangle.addTop = function(top){		if(!box.tops){			box.tops = [];		}		box.tops.push(top);	}		rrectangle.kn.getHeight = function(){		return box.getHeight();	}	rrectangle.kn.setHeight = function(height){		box.setHeight(height);	}	return rrectangle;		}