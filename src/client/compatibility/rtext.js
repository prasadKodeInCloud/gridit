function RText(options){	var defaults = {		styles: {			x: 0,			y: 0,			text: 'Label',			fontSize: 20,			draggable: true,			fontFamily: 'Calibri',			fill: 'green',			align:''		}	}		var settings = $.extend( true, {}, defaults, options );		var self = new Kinetic.Text(settings.styles);	setitemMouseEvents(self);		function setitemMouseEvents(item){		item.isSelected = false;		item.on('mouseover', function() {			document.body.style.cursor = 'pointer';		});				item.on('mouseout', function() {			document.body.style.cursor = 'default';		});		  		item.on('mousedown', function() {			if(item.isSelected){				item.setFill(settings.styles.fill);			}			else			{				item.setFill('red');				ItemPool.selectItem(item);			}						item.isSelected = !item.isSelected;		});			}		self.getCurrentX = function(){		return self.getX();	};		self.getCurrentY = function(){		return self.getY();	};	return{		kn:self	}}